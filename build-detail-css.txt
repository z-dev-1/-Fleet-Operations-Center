// build-detail-css.js — writes the command center CSS block
const fs = require('fs');
const cssPath = __dirname + '/renderer/src/css/fleet.css';
const MARKER = '/* COMMAND CENTER UNIT DETAIL';

const block = `

/* =================================================================
   COMMAND CENTER UNIT DETAIL — v4
   Header bar, vitals strip, tab system, repair tab, intelligence
   ================================================================= */

/* ── Panel shell ── */
.detail-panel{position:fixed;top:0;right:0;bottom:0;width:460px;background:var(--panel);border-left:1px solid var(--bdrs);box-shadow:-12px 0 40px rgba(0,0,0,.55);z-index:100;transform:translateX(100%);transition:transform .35s cubic-bezier(.22,1,.36,1);display:flex;flex-direction:column;overflow:hidden}
.detail-panel--open{transform:translateX(0)}
.detail-panel::before{content:'';position:absolute;top:0;left:0;bottom:0;width:1px;background:linear-gradient(180deg,var(--acc),transparent 40%,transparent 60%,var(--pur));opacity:.5}

/* ── Vitals Header ── */
.dp-header{flex-shrink:0;padding:14px 16px 12px;border-bottom:1px solid var(--bdr);position:relative;overflow:hidden}
.dp-header--unavailable{background:rgba(255,123,114,.06);border-left:3px solid var(--red)}
.dp-header--risk{background:rgba(255,166,87,.05);border-left:3px solid var(--org)}
.dp-header--active{background:transparent;border-left:3px solid var(--grn)}
.dp-header__scan{position:absolute;top:0;left:0;right:0;height:1px;background:linear-gradient(90deg,transparent,rgba(88,166,255,.4),transparent);animation:scanline 6s linear infinite;pointer-events:none}
.dp-header__top{display:flex;align-items:flex-start;gap:10px;margin-bottom:8px}
.dp-header__id{font-family:var(--mono);font-size:20px;font-weight:800;color:var(--txt);line-height:1}
.dp-header__state-badge{padding:3px 10px;border-radius:20px;font-size:10px;font-weight:700;letter-spacing:.5px;text-transform:uppercase;flex-shrink:0;margin-top:3px}
.dp-header__state-badge--unavailable{background:var(--redd);color:var(--red);border:1px solid rgba(255,123,114,.3)}
.dp-header__state-badge--active{background:var(--grnd);color:var(--grn);border:1px solid rgba(126,231,135,.3)}
.dp-header__close{margin-left:auto;width:24px;height:24px;border-radius:6px;border:1px solid var(--bdr);background:transparent;color:var(--mut);cursor:pointer;display:flex;align-items:center;justify-content:center;font-size:14px;transition:all .2s;flex-shrink:0}
.dp-header__close:hover{border-color:var(--red);color:var(--red);background:var(--redd)}
.dp-header__meta{font-size:11px;color:var(--txt2);margin-bottom:8px}
.dp-header__meta span+span::before{content:' · ';color:var(--mut)}
.dp-header__vitals{display:flex;align-items:center;gap:6px;flex-wrap:wrap}

/* ── Vital pills ── */
.dp-vital{display:flex;align-items:center;gap:5px;background:var(--el);border:1px solid var(--bdr);border-radius:6px;padding:4px 9px;font-size:10px;font-weight:600;color:var(--txt2)}
.dp-vital__icon{font-size:11px}
.dp-vital--red{border-color:rgba(255,123,114,.35);color:var(--red);background:var(--redd)}
.dp-vital--org{border-color:rgba(255,166,87,.35);color:var(--org);background:var(--orgd)}
.dp-vital--grn{border-color:rgba(126,231,135,.35);color:var(--grn);background:var(--grnd)}
.dp-vital--acc{border-color:rgba(88,166,255,.35);color:var(--acc2);background:var(--adim)}
.dp-header__launchers{display:flex;gap:5px;margin-left:auto}
.dp-launcher{width:28px;height:28px;border-radius:7px;border:1px solid var(--bdr);background:var(--el);color:var(--txt2);cursor:pointer;font-size:12px;display:flex;align-items:center;justify-content:center;transition:all .2s;flex-shrink:0;text-decoration:none}
.dp-launcher:hover{border-color:var(--acc);color:var(--acc2);background:var(--adim)}

/* ── Status Band ── */
.dp-status-band{flex-shrink:0;padding:8px 16px;border-bottom:1px solid var(--bdr);background:rgba(88,166,255,.03);font-size:11px;color:var(--txt2);line-height:1.55;display:flex;align-items:flex-start;gap:8px;min-height:36px}
.dp-status-band__icon{font-size:13px;flex-shrink:0;margin-top:1px}
.dp-status-band__text{flex:1}
.dp-status-band--loading .dp-status-band__text{color:var(--mut);font-style:italic}
.dp-status-band--stale .dp-status-band__text{color:var(--org)}

/* ── Tab Bar ── */
.dp-tabs{flex-shrink:0;display:flex;gap:1px;padding:8px 12px 0;border-bottom:1px solid var(--bdr);background:var(--panel)}
.dp-tab{padding:5px 13px;border-radius:6px 6px 0 0;font-size:10px;font-weight:600;font-family:var(--mono);color:var(--mut);cursor:pointer;transition:all .15s;border:1px solid transparent;border-bottom:none;letter-spacing:.3px;text-transform:uppercase;position:relative}
.dp-tab:hover{color:var(--txt);background:var(--el)}
.dp-tab.active{color:var(--acc2);background:var(--adim);border-color:rgba(88,166,255,.25)}
.dp-tab.active::after{content:'';position:absolute;bottom:-1px;left:20%;right:20%;height:2px;background:var(--acc);border-radius:1px}
.dp-tab__badge{display:inline-flex;align-items:center;justify-content:center;margin-left:5px;min-width:14px;height:14px;border-radius:7px;font-size:8px;font-weight:800;padding:0 3px;background:var(--el);color:var(--mut)}
.dp-tab.active .dp-tab__badge{background:rgba(88,166,255,.25);color:var(--acc2)}
.dp-tab__badge--red{background:var(--redd)!important;color:var(--red)!important}

/* ── Tab Panes ── */
.dp-body{flex:1;overflow-y:auto;display:flex;flex-direction:column}
.dp-pane{display:none;flex-direction:column;padding:14px 16px;gap:14px}
.dp-pane.active{display:flex}
.dp-section-title{font-family:var(--mono);font-size:8px;letter-spacing:1.6px;text-transform:uppercase;color:var(--mut);font-weight:700;display:flex;align-items:center;gap:8px;margin-bottom:8px}
.dp-section-title::before{content:'';width:10px;height:1px;background:var(--acc)}

/* ── Down Timer ── */
.dp-down-timer{font-family:var(--mono);font-size:13px;font-weight:800;color:var(--red);letter-spacing:.5px}
.dp-down-timer.warn{color:var(--org)}
.dp-down-timer.ok{color:var(--grn)}

/* ── ETC Bar ── */
.dp-etc-wrap{background:var(--card);border:1px solid var(--bdr);border-radius:8px;padding:12px 14px}
.dp-etc-title{font-size:10px;color:var(--txt2);margin-bottom:10px;display:flex;justify-content:space-between;align-items:center}
.dp-etc-title span:last-child{font-family:var(--mono);font-size:9px;color:var(--mut)}
.dp-etc-track{position:relative;height:6px;background:var(--el);border-radius:3px;margin-bottom:8px}
.dp-etc-fill{position:absolute;left:0;top:0;height:100%;border-radius:3px;transition:width .6s ease}
.dp-etc-fill--ok{background:linear-gradient(90deg,var(--grn),#38bdf8)}
.dp-etc-fill--warn{background:linear-gradient(90deg,var(--org),var(--ylw))}
.dp-etc-fill--overdue{background:linear-gradient(90deg,var(--red),#ff4444);animation:slaPulse 1.4s ease-in-out infinite}
.dp-etc-marker{position:absolute;top:-3px;width:2px;height:12px;background:var(--acc);border-radius:1px;opacity:.8}
.dp-etc-labels{display:flex;justify-content:space-between;font-size:9px;color:var(--mut);font-family:var(--mono)}

/* ── WO Card ── */
.dp-wo-card{background:var(--card);border:1px solid var(--bdr);border-radius:8px;padding:12px 14px;display:flex;flex-direction:column;gap:8px}
.dp-wo-card__header{display:flex;align-items:center;gap:8px}
.dp-wo-card__vendor{font-weight:700;font-size:12px;color:var(--txt);flex:1}
.dp-wo-card__status-pill{padding:2px 9px;border-radius:12px;font-size:9px;font-weight:700;text-transform:uppercase;letter-spacing:.4px}
.dp-wo-card__status-pill--open{background:var(--adim);color:var(--acc2);border:1px solid rgba(88,166,255,.25)}
.dp-wo-card__status-pill--sourcing{background:var(--orgd);color:var(--org);border:1px solid rgba(255,166,87,.3)}
.dp-wo-card__status-pill--closed{background:var(--el);color:var(--mut);border:1px solid var(--bdr)}
.dp-wo-card__desc{font-size:11px;color:var(--txt2);line-height:1.5}
.dp-wo-card__meta{display:flex;gap:12px;font-size:9px;color:var(--mut);font-family:var(--mono);flex-wrap:wrap}
.dp-wo-card__stale{color:var(--red);font-weight:700}
.dp-wo-card__link{font-size:10px;color:var(--acc2);text-decoration:none;margin-left:auto;flex-shrink:0}
.dp-wo-card__link:hover{text-decoration:underline}

/* ── Conversation Timeline ── */
.dp-convo{display:flex;flex-direction:column;gap:8px}
.dp-convo-msg{display:flex;gap:8px;align-items:flex-start}
.dp-convo-msg--carrier{flex-direction:row-reverse}
.dp-convo-bubble{max-width:78%;padding:8px 11px;border-radius:10px;font-size:11px;line-height:1.55;color:var(--txt)}
.dp-convo-msg--vendor .dp-convo-bubble{background:var(--card);border:1px solid var(--bdr);border-top-left-radius:3px}
.dp-convo-msg--carrier .dp-convo-bubble{background:var(--adim);border:1px solid rgba(88,166,255,.2);border-top-right-radius:3px}
.dp-convo-meta{font-size:9px;color:var(--mut);margin-top:3px;font-family:var(--mono)}
.dp-convo-msg--carrier .dp-convo-meta{text-align:right}
.dp-convo-av{width:22px;height:22px;border-radius:50%;flex-shrink:0;display:flex;align-items:center;justify-content:center;font-size:9px;font-weight:700;margin-top:4px}
.dp-convo-av--vendor{background:var(--orgd);color:var(--org);border:1px solid rgba(255,166,87,.3)}
.dp-convo-av--carrier{background:var(--adim);color:var(--acc2);border:1px solid rgba(88,166,255,.3)}
.dp-convo-show-more{font-size:10px;color:var(--acc2);background:none;border:1px solid var(--bdr);border-radius:6px;padding:4px 12px;cursor:pointer;transition:all .15s;width:100%}
.dp-convo-show-more:hover{background:var(--adim);border-color:var(--acc)}

/* ── Offsite Event Card ── */
.dp-offsite-card{background:var(--card);border:1px solid var(--bdr);border-radius:8px;padding:12px 14px;display:flex;flex-direction:column;gap:6px}
.dp-offsite-card__header{display:flex;align-items:center;gap:8px}
.dp-offsite-card__src-badge{font-size:9px;font-weight:700;padding:2px 8px;border-radius:10px;text-transform:uppercase;letter-spacing:.4px}
.dp-offsite-card__src-badge--case{background:var(--adim);color:var(--acc2)}
.dp-offsite-card__src-badge--estimate{background:var(--orgd);color:var(--org)}
.dp-offsite-card__src-badge--service_request{background:var(--purd);color:var(--pur)}
.dp-offsite-card__src-badge--unknown{background:var(--el);color:var(--mut)}
.dp-offsite-card__link{font-size:11px;font-weight:600;color:var(--acc2);text-decoration:none}
.dp-offsite-card__link:hover{text-decoration:underline}
.dp-offsite-card__stale{font-size:9px;color:var(--org);font-family:var(--mono);background:var(--orgd);padding:2px 8px;border-radius:10px;border:1px solid rgba(255,166,87,.3);margin-left:auto}
.dp-offsite-card__ts{font-size:9px;color:var(--mut);font-family:var(--mono)}

/* ── Risk Dial ── */
.dp-risk-wrap{background:var(--card);border:1px solid var(--bdr);border-radius:8px;padding:14px;display:flex;align-items:center;gap:16px}
.dp-risk-dial{position:relative;width:72px;height:72px;flex-shrink:0}
.dp-risk-dial svg{width:72px;height:72px}
.dp-risk-dial__num{position:absolute;inset:0;display:flex;align-items:center;justify-content:center;font-family:var(--mono);font-size:18px;font-weight:800}
.dp-risk-dial__num--high{color:var(--red)}
.dp-risk-dial__num--medium{color:var(--ylw)}
.dp-risk-dial__num--low{color:var(--grn)}
.dp-risk-info{flex:1}
.dp-risk-label{font-size:11px;font-weight:700;color:var(--txt);margin-bottom:4px}
.dp-risk-sub{font-size:10px;color:var(--txt2);line-height:1.5}
.dp-subsystem-row{display:flex;align-items:center;gap:8px;padding:3px 0}
.dp-subsystem-name{font-size:10px;color:var(--txt2);width:120px;flex-shrink:0}
.dp-subsystem-bar{flex:1;height:4px;background:var(--el);border-radius:2px;overflow:hidden}
.dp-subsystem-fill{height:100%;border-radius:2px;transition:width .6s ease}
.dp-subsystem-val{font-family:var(--mono);font-size:9px;font-weight:700;width:26px;text-align:right}

/* ── Insight Cards ── */
.dp-insight-card{background:var(--card);border:1px solid var(--bdr);border-radius:8px;padding:10px 12px;display:flex;flex-direction:column;gap:5px}
.dp-insight-card__header{display:flex;align-items:center;gap:7px}
.dp-insight-card__type{font-size:8px;font-weight:700;padding:2px 7px;border-radius:9px;text-transform:uppercase;letter-spacing:.4px;background:var(--purd);color:var(--pur)}
.dp-insight-card__summary{font-size:11px;color:var(--txt);line-height:1.5}
.dp-insight-card__action{font-size:10px;color:var(--acc2);font-weight:600}

/* ── Screenshot viewer ── */
.dp-screenshot-wrap{border-radius:8px;overflow:hidden;border:1px solid var(--bdr);cursor:zoom-in;position:relative}
.dp-screenshot-wrap img{width:100%;display:block}
.dp-screenshot-overlay{position:absolute;inset:0;background:linear-gradient(180deg,transparent 60%,rgba(13,17,23,.8));display:flex;align-items:flex-end;padding:8px}
.dp-screenshot-label{font-size:9px;color:var(--txt2);font-family:var(--mono)}

/* ── AI Briefing ── */
.dp-ai-brief{background:rgba(88,166,255,.06);border:1px solid rgba(88,166,255,.18);border-left:3px solid var(--acc);border-radius:8px;padding:12px 14px;font-size:11px;line-height:1.65;color:var(--txt);position:relative}
.dp-ai-brief__stamp{font-family:var(--mono);font-size:9px;color:var(--mut);margin-top:8px;display:flex;align-items:center;gap:5px}
.dp-ai-brief__stamp::before{content:'';width:5px;height:5px;border-radius:50%;background:var(--acc)}
.dp-ai-brief__loading{color:var(--mut);font-style:italic}
.dp-ai-brief__action{color:var(--grn);font-weight:600;margin-top:6px;font-size:11px}

/* ── Ask Orcha ── */
.dp-ask-row{display:flex;gap:6px;align-items:center;padding:8px 0;border-top:1px solid var(--bdr)}
.dp-ask-input{flex:1;background:var(--el);border:1px solid var(--bdr);border-radius:7px;padding:7px 11px;color:var(--txt);font-size:11px;outline:none;transition:all .2s;font-family:var(--font)}
.dp-ask-input:focus{border-color:var(--acc);box-shadow:0 0 0 3px rgba(88,166,255,.1)}
.dp-ask-input::placeholder{color:var(--mut)}
.dp-ask-chips{display:flex;gap:5px;flex-wrap:wrap;margin-bottom:6px}
.dp-ask-chip{font-size:9px;padding:3px 9px;border-radius:12px;border:1px solid var(--bdr);background:var(--el);color:var(--txt2);cursor:pointer;transition:all .15s}
.dp-ask-chip:hover{border-color:var(--acc);color:var(--acc2);background:var(--adim)}

/* ── Actions Grid ── */
.dp-action-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px}
.dp-action-btn{padding:10px 12px;border-radius:8px;border:1px solid var(--bdr);background:var(--el);color:var(--txt);font-size:11px;font-weight:600;cursor:pointer;text-align:center;transition:all .2s;display:flex;flex-direction:column;align-items:center;gap:4px}
.dp-action-btn:hover{border-color:var(--acc);background:var(--adim);transform:translateY(-1px)}
.dp-action-btn--primary{background:var(--adim);border-color:var(--acc);color:var(--acc2)}
.dp-action-btn--danger{border-color:rgba(255,123,114,.25);color:var(--red)}
.dp-action-btn--danger:hover{border-color:var(--red);background:var(--redd)}
.dp-action-btn__icon{font-size:16px}
.dp-action-btn__sub{font-size:9px;color:var(--mut);font-weight:400}

/* ── Notes ── */
.dp-notes-area{width:100%;min-height:100px;background:var(--el);color:var(--txt);border:1px solid var(--bdr);border-radius:7px;padding:9px 11px;font-size:11px;font-family:var(--font);resize:vertical;box-sizing:border-box;outline:none;transition:border-color .2s}
.dp-notes-area:focus{border-color:var(--acc);box-shadow:0 0 0 3px rgba(88,166,255,.08)}
.dp-notes-saved{font-size:9px;color:var(--grn);font-family:var(--mono);opacity:0;transition:opacity .3s}
.dp-notes-saved.visible{opacity:1}

/* ── Sync rows ── */
.dp-sync-row{display:flex;align-items:center;gap:8px;padding:5px 0;border-bottom:1px solid rgba(48,54,61,.5)}
.dp-sync-row:last-child{border-bottom:none}
.dp-sync-source{font-size:10px;color:var(--txt2);width:70px;flex-shrink:0}
.dp-sync-time{font-family:var(--mono);font-size:10px;color:var(--mut)}
.dp-sync-dot{width:6px;height:6px;border-radius:50%;flex-shrink:0}
.dp-sync-dot--ok{background:var(--grn)}
.dp-sync-dot--old{background:var(--org)}
.dp-sync-dot--none{background:var(--mut)}

/* ── LC form ── */
.dp-lc-form{display:flex;flex-direction:column;gap:7px}
.dp-lc-row{display:flex;gap:7px}

/* ── Empty states ── */
.dp-empty-state{text-align:center;padding:28px 16px;color:var(--mut);font-size:11px;display:flex;flex-direction:column;align-items:center;gap:8px}
.dp-empty-state__icon{font-size:26px;opacity:.4}

/* ── AI result ── */
.dp-ai-result-box{background:var(--card);border:1px solid var(--bdr);border-radius:8px;padding:12px 14px;font-size:11px;line-height:1.65;color:var(--txt2);white-space:pre-wrap;word-break:break-word}
.dp-ai-result-footer{display:flex;justify-content:flex-end;margin-top:8px}
`;

let css = fs.readFileSync(cssPath, 'utf8');
const cut = css.indexOf('\n/* COMMAND CENTER UNIT DETAIL');
if (cut > -1) css = css.slice(0, cut);
fs.writeFileSync(cssPath, css + block, 'utf8');
console.log('CSS written, size:', fs.statSync(cssPath).size);
